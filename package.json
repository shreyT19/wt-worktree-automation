{
  "name": "wt",
  "version": "0.1.0",
  "description": "Worktree automation — auto-install deps and set up environments when creating git worktrees",
  "type": "module",
  "bin": {
    "wt": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "lint": "bun x tsc --noEmit"
  },
  "dependencies": {
    "picocolors": "^1.1.1",
    "smol-toml": "^1.3.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.0"
  }
}
